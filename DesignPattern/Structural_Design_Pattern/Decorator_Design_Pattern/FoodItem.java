package DesignPattern.Structural_Design_Pattern.Decorator_Design_Pattern;

//Each component can be used on its own and wrapped by a decorator
public interface FoodItem {
    String getDescription();
    double getCost();
}
